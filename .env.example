# Backend
# [Append all Node.js server configurations, operational ports, and private API keys below]
PORT=5000
DATABASE_URL=postgresql://edu_user:edu_secure_pass@localhost:5432/eduprocess_local?schema=public

# Frontend
# [Append all React/Vite interface network targets and public keys below.]
VITE_API_URL=http://localhost:5000

# Others
# [Append all infrastructure variables, Docker container names, and shared external credentials below]
POSTGRES_USER=edu_user
POSTGRES_PASSWORD=edu_secure_pass
POSTGRES_DB=eduprocess_local
POSTGRES_PORT=5432